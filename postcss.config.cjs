/**
 * PostCSS configuration
 * 
 * This file configures PostCSS plugins for the project.
 * Current plugins:
 * - tailwindcss: For processing Tailwind CSS
 * - autoprefixer: For adding vendor prefixes to CSS
 */

module.exports = {
  plugins: {
    tailwindcss: {}, // Use default Tailwind CSS configuration
    autoprefixer: {}, // Use default Autoprefixer configuration
  },
};